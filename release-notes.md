Not signed.

Requires .NET 8 runtime. Probably, the app will tell itself if you don't have it.
Other than that - it's portable version, installation is not required.

What's new:
- File associations helper: sets icon for txt files and adds the app to the "Open with" list.
- Unsupported characters warning on save: added info about which exactly character causes the problem